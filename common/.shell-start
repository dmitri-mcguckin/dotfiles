#!/bin/bash

# Load shell
clear
neofetch
echo -e "Today's date is: $(date)\n" | lolcat

# Start the shell if on TTY1
if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
	clear
	sl | lolcat
	startx
fi
